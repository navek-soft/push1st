app-test:
  name: "Default Application for tests"
  key: "app-key"
  secret: "secret"
  options: { client-messages: true, statistic: false }
  channels: [ public, private, presence, protected ]
  origins: [ "localhost" ]
  hook:
    trigger: [ register, unregister, join, leave, push ]
    endpoint:
        - http://127.0.0.1:6002/
        - http://localhost:6002/